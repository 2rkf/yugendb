{
  "name": "@yugendb/sqlite",
  "version": "0.1.2",
  "description": "SQLite TypeScript driver for yugendb key-value and document storage.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "clean": "rm -rf dist coverage",
    "format": "tsc --noEmit",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "pnpm typecheck && pnpm test",
    "prepack": "pnpm build"
  },
  "dependencies": {
    "@yugendb/core": "workspace:*"
  },
  "keywords": [
    "database",
    "key-value",
    "storage",
    "driver"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/2rkf/yugendb.git",
    "directory": "packages/sqlite"
  },
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false,
  "engines": {
    "node": ">=22.5.0"
  }
}
