[package]
name = "rust-memory"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
chrono.workspace = true
serde.workspace = true
tokio.workspace = true
yugendb = { path = "../../crates/yugendb", features = ["memory"] }
