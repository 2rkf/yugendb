# yugendb

User-facing Rust facade crate for yugendb.

Optional driver modules are enabled with Cargo features such as `sqlite`, `memory`, `mysql`, `mongodb`, `postgres`, and `redis`.

## Install

```toml
[dependencies]
yugendb = "0.1.0"
```

## Licence

MIT
