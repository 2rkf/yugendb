# TypeScript driver contract

TypeScript drivers implement the `Driver` interface from `@yugendb/core` and report supported behaviour through `Capabilities`.

External-service drivers require their matching service connection string for integration tests.
