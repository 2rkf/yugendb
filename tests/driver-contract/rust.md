# Rust driver contract

Rust drivers implement `yugendb_core::Driver` and report supported behaviour through `Capabilities`.

External-service drivers require their matching service connection string for integration tests.
